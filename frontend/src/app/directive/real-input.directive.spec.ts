import { RealInputDirective } from './real-input.directive';

describe('RealInputDirective', () => {
  it('should create an instance', () => {
    const directive = new RealInputDirective();
    expect(directive).toBeTruthy();
  });
});
